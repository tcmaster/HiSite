<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 有焦点 -->
    <item android:state_checked="true" android:state_enabled="true"><shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">

            <!-- stroke：描边 -->
            <stroke android:width="0.5dip" android:color="@color/white" />

            <!-- 填充的颜色 -->
            <solid android:color="@color/white" />
            <!-- 设置矩形的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:bottomLeftRadius="4dp" android:topLeftRadius="4dp" />
        </shape></item>
    <!-- 默认 -->
    <item><shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">

            <!-- stroke：描边 -->
            <stroke android:width="0.5dip" android:color="@color/white" />

            <!-- 填充的颜色 -->
            <solid android:color="@color/transparent" />
            <!-- 设置矩形的四个角为弧形 -->
            <!-- android:radius 弧形的半径 -->
            <corners android:bottomLeftRadius="4dp" android:topLeftRadius="4dp" />
        </shape></item>

</selector>