package com.android.tonight8.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.android.tonight8.dao.entity.Event;

import com.android.tonight8.dao.entity.PopGoods;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table POP_GOODS.
*/
public class PopGoodsDao extends AbstractDao<PopGoods, Long> {

    public static final String TABLENAME = "POP_GOODS";

    /**
     * Properties of entity PopGoods.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property Rid = new Property(1, Long.class, "rid", false, "RID");
        public final static Property PopGoodsPrice = new Property(2, Integer.class, "popGoodsPrice", false, "POP_GOODS_PRICE");
        public final static Property PopGoodsName = new Property(3, String.class, "popGoodsName", false, "POP_GOODS_NAME");
        public final static Property PopGoodsPic = new Property(4, String.class, "popGoodsPic", false, "POP_GOODS_PIC");
    };

    private DaoSession daoSession;


    public PopGoodsDao(DaoConfig config) {
        super(config);
    }
    
    public PopGoodsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'POP_GOODS' (" + //
                "'ID' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'RID' INTEGER," + // 1: rid
                "'POP_GOODS_PRICE' INTEGER," + // 2: popGoodsPrice
                "'POP_GOODS_NAME' TEXT," + // 3: popGoodsName
                "'POP_GOODS_PIC' TEXT);"); // 4: popGoodsPic
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'POP_GOODS'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PopGoods entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long rid = entity.getRid();
        if (rid != null) {
            stmt.bindLong(2, rid);
        }
 
        Integer popGoodsPrice = entity.getPopGoodsPrice();
        if (popGoodsPrice != null) {
            stmt.bindLong(3, popGoodsPrice);
        }
 
        String popGoodsName = entity.getPopGoodsName();
        if (popGoodsName != null) {
            stmt.bindString(4, popGoodsName);
        }
 
        String popGoodsPic = entity.getPopGoodsPic();
        if (popGoodsPic != null) {
            stmt.bindString(5, popGoodsPic);
        }
    }

    @Override
    protected void attachEntity(PopGoods entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PopGoods readEntity(Cursor cursor, int offset) {
        PopGoods entity = new PopGoods( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // rid
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // popGoodsPrice
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // popGoodsName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // popGoodsPic
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PopGoods entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setRid(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setPopGoodsPrice(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setPopGoodsName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setPopGoodsPic(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PopGoods entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PopGoods entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getEventDao().getAllColumns());
            builder.append(" FROM POP_GOODS T");
            builder.append(" LEFT JOIN EVENT T0 ON T.'RID'=T0.'ID'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PopGoods loadCurrentDeep(Cursor cursor, boolean lock) {
        PopGoods entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Event event = loadCurrentOther(daoSession.getEventDao(), cursor, offset);
        entity.setEvent(event);

        return entity;    
    }

    public PopGoods loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PopGoods> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PopGoods> list = new ArrayList<PopGoods>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PopGoods> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PopGoods> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
